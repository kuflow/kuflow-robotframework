{
  "name": "kuflow-rest-client",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "2.4.26"
  },
  "scripts": {
    "format": "poetry run black kuflow_rest_client",
    "openapi": "poetry run npx @openapitools/openapi-generator-cli generate -i etc/openapi.yaml -g python --additional-properties=generateSourceCodeOnly=true,packageName=kuflow_rest_client,packageVersion=0.1.0,hideGenerationTimestamp=true",
    "generate": "npm run openapi && npm run format"
  }
}
